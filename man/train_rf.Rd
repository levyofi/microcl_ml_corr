% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_rf.R
\name{train_rf}
\alias{train_rf}
\title{Train a Random Forest model for residual correction}
\usage{
train_rf(
  train_X,
  train_y,
  num_trees = 500,
  tune = TRUE,
  n_combinations = 5,
  max_depth_options = c(10, 20, 30, 0),
  min_node_size_options = c(2, 5, 10),
  mtry_options = NULL,
  val_X = NULL,
  val_y = NULL,
  seed = 123
)
}
\arguments{
\item{train_X}{Data.frame or matrix of training features}

\item{train_y}{Numeric vector of training targets (residuals)}

\item{num_trees}{Number of trees (default 500)}

\item{tune}{Logical. If TRUE, try multiple hyperparameter combinations.}

\item{n_combinations}{Number of random HP combinations to try when tune=TRUE}

\item{max_depth_options}{Integer vector of max.depth values to search}

\item{min_node_size_options}{Integer vector of min.node.size values}

\item{mtry_options}{Values for mtry (NULL means use defaults)}

\item{val_X}{Optional validation features (for dedicated val set tuning)}

\item{val_y}{Optional validation targets}

\item{seed}{Random seed}
}
\value{
A fitted ranger model
}
\description{
Trains a ranger Random Forest to predict residuals (measured - predicted).
Optionally performs hyperparameter tuning via out-of-bag error.
}
